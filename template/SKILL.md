---
name: template-skill
description: Replace with a description of the skill and when Claude should use it. Front-load trigger words — this string is all the model sees when deciding whether to invoke the skill.
---

# Insert instructions below

Keep the body short. Push detail into sibling files (`reference.md`, `scripts/`)
and link to them, so the model loads them only when needed (progressive disclosure).
